{% extends 'layout_base.html.twig' %}

{% block titre_page %} INFORNATIONS CLIENT {% endblock %}
{% block sous_titre_page %} le client {% endblock %}
{% block nouveau %}<a href="{{ path('clients_new') }}">Nouveau</a>{% endblock %}

 {% block contenuprincipal %}
       <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>NomClient</th>
                <td>{{ client.NomClient }}</td>
            </tr>
            <tr>
                <th>PrenomClient</th>
                <td>{{ client.PrenomClient }}</td>
            </tr>
            <tr>
                <th>Telephone1</th>
                <td>{{ client.Telephone1 }}</td>
            </tr>
            <tr>
                <th>Telephone2</th>
                <td>{{ client.Telephone2 }}</td>
            </tr>
            <tr>
                <th>ZoneGeographique</th>
                <td>{{ client.ZoneGeographique }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ client.Sexe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('clients_index') }}">back to list</a>

    <a href="{{ path('clients_edit', {'id': client.id}) }}">edit</a>

    {{ include('clients/_delete_form.html.twig') }}
{% endblock %}