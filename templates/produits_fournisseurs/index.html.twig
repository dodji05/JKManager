{% extends 'layout_base.html.twig' %}

{% block titre_page %} PRODUITS - FOURNISSEURS {% endblock %}
{% block sous_titre_page %} produits/fournisseur {% endblock %}
{% block nouveau %}<a href="{{ path('produits_fournisseurs_new') }}">Nouveau</a>{% endblock %}

 {% block contenuprincipal %}
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produits_fournisseur in produits_fournisseurs %}
            <tr>
                <td>{{ produits_fournisseur.id }}</td>
                <td>{{ produits_fournisseur.Prix }}</td>
                <td>
                    <a href="{{ path('produits_fournisseurs_show', {'id': produits_fournisseur.id}) }}">show</a>
                    <a href="{{ path('produits_fournisseurs_edit', {'id': produits_fournisseur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('produits_fournisseurs_new') }}">Create new</a>
{% endblock %}