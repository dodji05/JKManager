<form method="post" action="{{ path('details_appro_delete', {'id': details_appro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_appro.id) }}">
    <button class="btn">Delete</button>
</form>